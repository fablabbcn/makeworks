<%= title 'Industries' %>

<% if @industry_taxonomies %>
  <%= render partial: 'taxonomy_list', locals: {the_item: @industry_taxonomies} %>
<% end %>

<% if current_user&.is_admin? %>
  <%= link_to 'New Industry Taxonomy', new_industry_taxonomy_path, class: 'btn btn-primary' %>
<% end %>
