<%= title t('shared.manufacturers') %>

<% if @manufacturer_taxonomies %>
  <%= render partial: 'taxonomy_list', locals: {the_item: @manufacturer_taxonomies} %>
<% end %>

<% if current_user&.is_admin? %>
  <%= link_to 'New Manufacturer Taxonomy', new_manufacturer_taxonomy_path, class: 'btn btn-primary' %>
<% end %>
