<!DOCTYPE html>
<html lang="en">

  <%= render 'head' %>

  <body>
    <%= render 'navbar' %>

    <%# flash_messages %>
    <%= render 'flasher' %>

    <div class="container-xxl py-5">
      <div class="card my-5 p-5 mx-auto" style="max-width: 700px">
        <%= yield %>
      </div>
    </div>

    <%= render 'footer' %>
  </body>
</html>
