<div class="my-4">
  <%= link_to "Processes", process_taxonomies_path %>
  <%= title @process_taxonomy %>
</div>

<div class="border p-4 my-4">
  <p>
  <strong>Name:</strong>
  <%= @process_taxonomy.name %>
  </p>

  <p>
  <strong>Parent:</strong>
  <%= @process_taxonomy.parent_id %>
  </p>

  <% if current_user&.is_admin? %>
    <%= link_to 'Edit', edit_process_taxonomy_path(@process_taxonomy), class: 'btn btn-primary' %>
  <% end %>
</div>


<%= render @process_taxonomy.companies %>
