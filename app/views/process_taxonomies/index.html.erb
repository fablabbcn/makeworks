<%= title 'Processes' %>

<% if @process_taxonomies %>
  <%= render partial: 'taxonomy_list', locals: {the_item: @process_taxonomies} %>
<% end %>

<% if current_user&.is_admin? %>
  <%= link_to 'New Process Taxonomy', new_process_taxonomy_path, class: 'btn btn-primary' %>
<% end %>
