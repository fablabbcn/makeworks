<%= title 'Machines' %>

<% if @machines_taxonomies %>
  <%= render partial: 'taxonomy_list', locals: {the_item: @machines_taxonomies} %>
<% end %>

<% if current_user&.is_admin? %>
  <%= link_to 'New Machine Taxonomy', new_machines_taxonomy_path, class: 'btn btn-primary' %>
<% end %>
